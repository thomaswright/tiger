// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Caml_obj from "rescript/lib/es6/caml_obj.js";
import * as OtherJs from "./other.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as Core__Array from "@rescript/core/src/Core__Array.res.mjs";
import * as Core__Option from "@rescript/core/src/Core__Option.res.mjs";
import * as Belt_MapString from "rescript/lib/es6/belt_MapString.js";
import DateSelectJsx from "./DateSelect.jsx";
import * as State from "@legendapp/state";
import StatusSelectJsx from "./StatusSelect.jsx";
import TigerSvg from "./assets/tiger.svg";
import * as UseLocalStorageJs from "./useLocalStorage.js";
import UseLocalStorageJs$1 from "./useLocalStorage.js";
import * as SupaLegendTs from "./utils/SupaLegend.ts";
import UseSessionStorageJs from "./useSessionStorage.js";

function mapNullable(n, f) {
  Core__Option.mapOr((n == null) ? undefined : Caml_option.some(n), undefined, f);
}

var TextareaAutosize = {};

function arrayToggle(v, el) {
  if (v.includes(el)) {
    return v.filter(function (x) {
                return Caml_obj.notequal(x, el);
              });
  } else {
    return v.concat([el]);
  }
}

function useLocalStorage(prim0, prim1) {
  return UseLocalStorageJs$1(prim0, prim1);
}

function useSessionStorage(prim0, prim1) {
  return UseSessionStorageJs(prim0, prim1);
}

function useLocalStorageListener(prim0, prim1) {
  return UseLocalStorageJs.useLocalStorageListener(prim0, prim1);
}

function focusPreviousClass(prim0, prim1) {
  OtherJs.focusPreviousClass(prim0, prim1);
}

function focusNextClass(prim0, prim1) {
  OtherJs.focusNextClass(prim0, prim1);
}

function addTodo(prim0, prim1, prim2) {
  return SupaLegendTs.addTodo(prim0, prim1, prim2);
}

function setTodoText(prim0, prim1) {
  SupaLegendTs.setTodoText(prim0, prim1);
}

function setTodoAdditionalText(prim0, prim1) {
  SupaLegendTs.setTodoAdditionalText(prim0, prim1);
}

function setTodoDate(prim0, prim1) {
  SupaLegendTs.setTodoDate(prim0, prim1);
}

function setTodoMode(prim0, prim1) {
  SupaLegendTs.setTodoMode(prim0, prim1);
}

function setTodoModesShown(prim0, prim1) {
  SupaLegendTs.setTodoModesShown(prim0, prim1);
}

function setTodoStatus(prim0, prim1) {
  SupaLegendTs.setTodoStatus(prim0, prim1);
}

function setTodoPosition(prim0, prim1, prim2) {
  SupaLegendTs.setTodoPosition(prim0, prim1, prim2);
}

function deleteTodo(prim) {
  SupaLegendTs.deleteTodo(prim);
}

var logoUrl = TigerSvg;

function deleteTodoAndMoveChildren(todoRelation) {
  var todo = todoRelation.self;
  State.batch(function () {
        SupaLegendTs.deleteTodo(todo.id);
        todoRelation.children.forEach(function (child, i) {
              setTodoPosition(child.id, todo.parent_todo, Core__Option.mapOr(todoRelation.sibs[todoRelation.index + 1 | 0], todo.position + i, (function (sib) {
                          var step = (sib.position - todo.position) / (todoRelation.children.length + 1);
                          return step + i;
                        })));
            });
      });
}

var make = StatusSelectJsx;

var StatusSelect = {
  make: make
};

var make$1 = DateSelectJsx;

var DateSelect = {
  make: make$1
};

function toNullableNull(o) {
  if (o !== undefined) {
    return Caml_option.valFromOption(o);
  } else {
    return null;
  }
}

function useDebounce(initialValue, onTrigger, delay) {
  var match = React.useState(function () {
        return initialValue;
      });
  var val = match[0];
  var delayedSetRef = React.useRef(undefined);
  React.useEffect((function () {
          Core__Option.mapOr(delayedSetRef.current, undefined, (function (x) {
                  clearTimeout(x);
                }));
          if (Caml_obj.notequal(val, initialValue)) {
            var id = setTimeout((function () {
                    onTrigger(val);
                  }), delay);
            delayedSetRef.current = Caml_option.some(id);
          }
          
        }), [val]);
  return [
          val,
          match[1]
        ];
}

function groupByAndSort(arr, groupByKey, sortByKey) {
  return Belt_MapString.map(Core__Array.reduce(arr, undefined, (function (a, c) {
                    var key = Core__Option.getOr(groupByKey(c), "root");
                    return Belt_MapString.update(a, key, (function (o) {
                                  if (o !== undefined) {
                                    return o.concat([c]);
                                  } else {
                                    return [c];
                                  }
                                }));
                  })), (function (v) {
                return v.toSorted(function (a, b) {
                            return sortByKey(a) - sortByKey(b);
                          });
              }));
}

export {
  mapNullable ,
  TextareaAutosize ,
  arrayToggle ,
  useLocalStorage ,
  useSessionStorage ,
  useLocalStorageListener ,
  focusPreviousClass ,
  focusNextClass ,
  addTodo ,
  setTodoText ,
  setTodoAdditionalText ,
  setTodoDate ,
  setTodoMode ,
  setTodoModesShown ,
  setTodoStatus ,
  setTodoPosition ,
  deleteTodo ,
  logoUrl ,
  deleteTodoAndMoveChildren ,
  StatusSelect ,
  DateSelect ,
  toNullableNull ,
  useDebounce ,
  groupByAndSort ,
}
/* logoUrl Not a pure module */
